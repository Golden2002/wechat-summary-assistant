# 更新日志

本文件记录各版本的主要变更。版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)规范。

---

## v1.0.0

首个公开版本。本版本在 [Vita0519/wechat_summary](https://github.com/Vita0519/wechat_summary)
的基础上完成微信 4.x 适配，并重写了消息读取层。

### 新增

- **滚动收集引擎**：重写消息读取层，使其能够完整覆盖用户所选定的时间范围。
  原版实现由于所依赖的 `wxauto4` 免费版缺失内部方法而中断翻页，只能读取当前屏幕上的
  少量消息；在相同的测试数据上，可读取的消息数量由 7 条提升至 95 条。
- **19 套场景化提示词模板**：覆盖通用、私人社交、工作协作、学习与行业、社区与运营
  五类场景。其中「信息分享群 · 每日信息汇总」以**信息条目**为整理单位，
  会自动合并被多人重复转发的信息，并保留每条信息的来源与链接。
- **Markdown 到微信纯文本的转换器**（`wechat_text.py`）：微信聊天窗口不渲染 Markdown，
  因此本工具会在显示与发送之前，把模型输出转换为以【标题】与 · 条目 组织的纯文本。
- **任务中止机制**：用户可以按 `Esc` 键、按空格键，或者点击「中止」按钮来停止正在
  执行的抓取任务。中止采用协作式实现，因此不会破坏 `wxauto4` 的 UI 自动化会话状态。
- **绝对时间范围**：除相对时间（最近 30 分钟至最近 30 天）外，还支持「今天 00:00 起」
  「昨天 00:00 起」「本周一 00:00 起」三个日历预设，以及自定义的起止时刻。
- **12 家 AI 服务的预设**：新增腾讯 TokenHub 与阶跃星辰两家，并把全部模型清单更新至
  2026 年 9 月 17 日核对的状态；界面会标注各家是否支持 `GET /models` 动态发现。
- **模板导入与导出**：可以把模板保存为 JSON 文件，以便分享或者迁移到另一台机器。
- **四套离线测试**：用一个「只能看到当前屏幕」的模拟微信对象，覆盖抓取引擎的全部关键假设。
- **技术说明文档的 PDF 版本**：由 `tools/md_to_html.py` 与 `tools/render_pdf.js` 生成，
  排版由 `tools/pdf/` 下的样式表与 HTML 模板控制。

### 修复

- **历史消息读取不全**：原版实现调用 `wx.LoadMoreCache()` 时，`wxauto4` 免费版内部会
  引用一个并不存在的 `WeChatMainWnd.load_more_message` 方法，异常被捕获之后翻页循环
  在第一轮就中断了。
- **重复内容被合并**：原版以 `(kind, sender, content)` 作为去重键，会把同一个人连续
  发出的两条相同消息误判为同一条。本版改用内容序列对齐的方式去重。
- **时间信息丢失**：原版不会把时间分隔条写入发送给模型的文本行，导致模型完全看不到
  时刻信息，而模板中要求标注时间的规则就成了空头承诺。本版会把时刻一并送入模型。
- **把总结再总结一遍**：通过记录已发送总结的指纹，在后续抓取时跳过这些消息。
- **内存化调用导致的卡顿**：为模型请求设置了明确的超时与重试策略，并在失败时给出
  可读的错误提示，而不是让界面长时间停留在无响应状态。

### 变更

- 依赖库由 `wxauto` 改为 `wxauto4`。前者已从 PyPI 下架，在任意 Python 版本上都无法安装。
- 内置提示词的默认输出结构保持不变，仍然以「通用群聊总结（原版）」作为未知场景的兜底。
- 三套已被取代的旧内置模板会在加载配置时自动摘除，摘除之前会把完整配置备份到
  `config/ai_config.backup-<时间戳>.json`。

### 文档

- 技术说明的 PDF 版式重做。封面改为贯通整页的深蓝书脊，并在标题下方增加「内容提要」，
  使封面既能说明文档回答什么问题，又不再留出大片空白。目录改为按章节边界配平的双栏排布。
- 目录与正文不再使用任何灰色分隔线。原版目录的每个条目下都带有一条横贯版心的灰线
  （正文的链接样式给整行的目录条目加了下边框），38 个条目就是 38 条线；本次改由字号、
  字重、缩进与色阶表达层级，并同步去掉了页脚的分隔线。
- 技术说明中的六张插图改由 Mermaid 图源生成（`tools/pdf/diagrams/*.mmd`），渲染出的 SVG
  会内联进 HTML，因此在 PDF 里是真正的矢量图形，放大到任意倍数都不会失真。
- 技术说明正文完成一次语言规范化修订，仅调整表述，不改动任何事实、数据与代码标识符。

### 已知限制

- 抓取的速度上限约为每秒 0.5 至 1 条消息。该上限由微信客户端决定，与所选用的模型无关。
- 一屏最多只能容纳 8 至 12 条消息，因为窗口高度会被屏幕高度限制。
- 仅支持 Windows 平台，并且需要微信 4.x 桌面客户端处于登录状态。

---

## 版本号说明

本项目在 v1.0.0 之前没有正式发布过版本，因此更新日志从 v1.0.0 开始记录。
