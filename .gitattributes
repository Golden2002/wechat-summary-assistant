# 默认按 LF 存储，检出时按平台转换
* text=auto eol=lf

# Windows 批处理必须用 CRLF，否则可能执行异常
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制（必须显式声明，避免 text=auto 误判后按行尾转换而损坏文件）
*.png binary
*.ico binary
*.jpg binary
*.gif binary
*.exe binary
*.pdf binary
*.zip binary
